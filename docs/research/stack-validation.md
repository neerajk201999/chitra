# Stack Validation (2026-07-15)

Decisions locked before implementation. Evidence: direct reads of HyperFrames engine source (`packages/engine/src/services/{browserManager,chunkEncoder,frameCapture}.ts`) + prior research reports.

1. **Frame capture — seek protocol + screenshot mode.** HyperFrames supports two modes: `beginframe` (CDP `HeadlessExperimental.beginFrame`, atomic compositor control, **Linux-only**) and `screenshot` (seek + `Page.captureScreenshot`, all platforms, their pooled default). Chitra v0 uses **screenshot mode** with the same contract they converged on: the compiled page exposes `window.__chitra = { durationMs, fps, seek(ms) }`; the renderer seeks, waits for paint settle, screenshots PNG. BeginFrame mode is a Linux/CI optimization to add later behind the same interface.
2. **Deterministic Chrome flags** (mined from HyperFrames): `--deterministic-mode --run-all-compositor-stages-before-draw --disable-threaded-animation --disable-threaded-scrolling --disable-checker-imaging --disable-image-animation-resync --font-render-hinting=none --force-color-profile=srgb --disable-background-timer-throttling --disable-renderer-backgrounding --hide-scrollbars` + fixed viewport with `deviceScaleFactor: 2` for crisp text downscaled to target.
3. **GSAP** (`gsap` on npm, 100% free incl. all former Club plugins since Webflow acquisition, redistributable). Deterministic drive: build one paused master timeline; `seek` implemented as `tl.time(t, true)` (suppressEvents) — never wall-clock `play()`. `lazy: false` on all tweens to avoid deferred first-render writes.
4. **FFmpeg encode ladder** (mirrors HyperFrames' presets): master H.264 `libx264 -preset slow -crf 15 -pix_fmt yuv420p` + BT.709 tags (`-colorspace bt709 -color_primaries bt709 -color_trc bt709`) with full→limited range conversion (Chrome screenshots are full-range sRGB); draft `-preset ultrafast -crf 28`. Frames piped via `image2pipe` (PNG), audio muxed after with `loudnorm=I=-14`. ProRes 4444 (`prores_ks -profile:v 4444`) as the lossless intermediate option.
5. **Fonts — bundled OFL via Fontsource npm packages**, inlined as base64 WOFF2 into the compiled page (offline + deterministic). v0 families: Inter (text/UI), Space Grotesk (display), Instrument Serif (editorial display), JetBrains Mono (code). All SIL OFL 1.1 — redistribution permitted. Render waits on `document.fonts.ready` **and** an explicit probe that each family is loaded.
6. **Seeded randomness**: mulberry32 (32-bit, 4-line, deterministic across platforms). Seed lives in IR meta; compiler threads it into any generative choreography.
7. **Per-frame text contrast (MO-TYPE-2)**: hybrid — pre-render, compiler computes static text/background contrast from resolved styles (cheap, exact); post-render, for text over images/video/gradients the gate samples the screenshot pixels inside each text bounding box (exported by the compiled page as `window.__chitra.textRegions(t)`) via sharp, computing WCAG ratio against the text color. Runs only on frames where text overlaps media layers.
8. **Evidence sheets**: sharp (libvips) composites — filmstrip at cut boundaries ±1 frame, timing labels drawn as SVG overlays, per-scene hero frames at full res. (ffmpeg tile filter as fallback; sharp chosen for labeling control — video-use's composite-evidence pattern.)
9. **React question settled**: nothing needed for launch-film motion graphics is lost by compiling IR→HTML/GSAP instead of React: component reuse happens at IR level (styles/presets), charts are compile-time-generated SVG (typed `chart` elements), layout is CSS grid — the browser is the layout engine either way. Remotion's React is a human-authoring affordance, not a rendering capability. No client framework ships in compiled pages.
10. **Audio (beat grid/ASR)**: deferred to M2 as planned; IR schema reserves `audio` now so no migration later. Candidate libs recorded: ffmpeg onset filters or essentia.js for beat grid; whisper.cpp optional for word timestamps.

Known risk accepted: screenshot mode is ~2-4 fps/worker; mitigated by per-scene parallelism + static-frame dedup (HyperFrames does the same) — both post-v0 optimizations behind the renderer interface.
